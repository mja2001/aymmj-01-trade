version: '3.8'
services:
  trading-app:
    build: .
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app
    ports:
      - "8080:8080"  # For dashboard if added
    command: python -m src.strategies.main --mode paper

  # Add Redis for caching, etc.
  redis:
    image: redis:alpine